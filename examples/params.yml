# A params file for examples/strategy.params.yml: a plain NAME: value mapping
# resolving that strategy's `!param` placeholders. Load it with `@`:
#
#   fugazi run --strategy @examples/strategy.params.yml \
#              --params @examples/params.yml \
#              --series @examples/candles.csv \
#              --output-dir out/
#
# `--params` is repeatable and mixes forms (like --series): a later
# `--params FAST=9` on the command line overrides the value from this file.
FAST: 5
SLOW: 20
SYM: BTC
